#include <stdio.h>                  // <libraries> looked within compilation include path vs "libraries" absolute path

int main(int argc, char *argv[])    // main() is expected to return an int, and (I think) takes args as number, and
{
    puts("Hello, world!");
    return 0;
}

